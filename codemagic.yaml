workflows:
  Deepse-vpn:
    name: DeepseVpn
    max_build_duration: 30
    instance_type: mac_mini
    scripts:
      - name: Build
        script: |
          echo "Building DeepseVpn APK..."
    artifacts:
      - build/**/*.apk
